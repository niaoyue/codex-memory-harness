# Codex 外脑插件执行文档

> 迁移说明：这份 Codex 生成的历史执行方案已经迁移到 `.codex/harness/backlog/execution-plan.md`。

`docs/` 只保留这个兼容入口，避免旧链接失效。后续维护早期架构来龙去脉、阶段目标和执行边界时，请更新：

```text
.codex/harness/backlog/execution-plan.md
```

当前任务清单和未完成 Task 进度以 `.codex/harness/backlog/tasks.md` 为准；正式规格与 change contract 位于 `openspec/`。
